<div class="w-full flex flex-col rounded-tr-xl">
    <div class="w-full h-12 flex items-center px-4 bg-white rounded-br-xl border-l border-r border-primary">
        <h2 class="font-medium text-md">My List</h2>
    </div>
    <div class="w-full flex-1 flex flex-col space-y-1 overflow-y-auto">
        <%= link_to "Dashboard", dashboard_path, class: "w-full #{is_active?("dashboards") ? "bg-primary text-white" : "bg-secondary hover:bg-primary hover:text-white text-sky-600"} py-2 px-4 font-medium rounded-tr-xl" %>
        <%= link_to "Products", products_path, class: "w-full #{is_active?("products") ? "bg-primary text-white" : "bg-secondary hover:bg-primary hover:text-white text-sky-600"} py-2 px-4 font-medium" %>
        <%= link_to "My Orders", orders_path, class: "w-full #{is_active?("orders") ? "bg-primary text-white" : "bg-secondary hover:bg-primary hover:text-white text-sky-600"} py-2 px-4 font-medium" %>
        <%= link_to "My Cart", cart_path, class: "w-full #{is_active?("carts") ? "bg-primary text-white" : "bg-secondary hover:bg-primary hover:text-white text-sky-600"} py-2 px-4 font-medium" %>
    </div>
    <div class="border-t-2 border-sky-300 flex flex-col space-y-4 p-4">
        <div class="flex items-center justify-between border border-sky-300 py-2 px-4">
            <h2 class="font-medium text-sm"><%= Current.user&.email || "Demo" %> status</h2>
            <div class="w-2 h-2 bg-green-400 rounded-full"></div>
        </div>
        <div class="flex items-center justify-between">
            <h2 class="font-medium text-sm"><%= Current.user&.email || "Demo" %></h2>
            <div class="">...</div>
        </div>
    </div>
</div>