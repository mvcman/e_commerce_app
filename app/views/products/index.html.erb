<div class="w-full h-full flex flex-col">
    <div class="w-full h-12 flex items-center justify-between p-4 border-b border-primary font-medium space-x-2">
        <div class=""><%= breadcrumbs %></div>
        <%= link_to "Create New", new_product_path, class: "py-1 px-2 text-sm bg-primary text-white rounded-md" %>
    </div>
    <div class="h-full w-full overflow-y-auto p-4 flex flex-col space-y-4">
        <% @products.each do |category, products| %>
            <%= render "products/category_products", category:, products: %>
        <% end %>
    </div>
</div>